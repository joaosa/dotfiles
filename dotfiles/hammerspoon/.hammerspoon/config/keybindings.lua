-----------------------------------------------
-- Key Modifier Definitions
-----------------------------------------------

local hyper = { "shift", "ctrl", "alt", "cmd" }
local altCmd = { "ctrl", "cmd" }

return {
    hyper = hyper,
    altCmd = altCmd,
}
